import React from 'react';
import './Hero.css'; // Import this CSS file

const Hero = () => (
  <section id="hero" className="hero">
    <h1 className="hero-title">Integrated AI Solutions for Defense</h1>
    <p className="hero-description">Empowering national security with cutting-edge AI technology tailored for defense needs.</p>
    <a href="#contact" className="hero-btn">Get in Touch</a>
  </section>
);

export default Hero;
